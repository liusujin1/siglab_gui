This directory intentionally contains only synthetic, non-controller data.

Use test-mock.json for an initial UI check. Real controller configurations,
tokens, recovery snapshots, and captured traces must remain outside the kit.
